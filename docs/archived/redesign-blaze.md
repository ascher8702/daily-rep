> **Archived 2026-06-28.** Completed Charge/Blaze redesign build-plan (it.1–20, all shipped/verified). The live UI plus the `tailwind.config.ts` / `globals.css` token layer are the source of truth now. Kept for history — not current.

# Redesign — "Charge / Blaze" (token-driven)

> **User directive (2026-06-24):** implement the redesign in `/Users/vlad/Downloads/Fitness app redesign styles`, following best FE/system-design principles — **reusable components, design tokens (tokenization)**. Chosen direction: **Charge / Blaze**. Scope: **replace** the current look, but **token-driven** (CSS-variable token layer + reusable components, so themes remain possible later). This is now the loop's HEADLINE goal — implemented incrementally, **never regressing shipped functionality, the progression engine, analytics, sync, auth, or the test suite.**

## Source assets
`/Users/vlad/Downloads/Fitness app redesign styles/`
- `Daily Rep Blaze.dc.html` (164KB) — the Charge/Blaze direction's full design (markup + inline CSS). PRIMARY reference for tokens + component styles.
- `Fitness Redesign.dc.html` (91KB) — all 4 directions (Apex/Daylight/Charge/Studio) on Today + Progress.
- `Daily Rep Logo - Charge Bolt.dc.html`, `Daily Rep Logos.dc.html` — the new "Charge Bolt" brand mark.
- `screenshots/blaze-*.png`, `overview.png`, `today-check.png`, `progress-row.png`, `new-brand.png` — the rendered screens.
- `ios-frame.jsx`, `support.js` — device frame + helper (reference only).

## The Charge / Blaze look (from the overview brief)
> "03 · Charge — Maximum energy. Heavy condensed type, oversized numerals, hot-blaze accent, chunky color-blocked cards."

Dark, high-energy, bold. Big condensed headings + oversized tabular numerals. Hot-orange "blaze" accent on chunky, color-blocked cards (e.g. an orange-filled "Today's workout" hero). Recovery shown as big % + color-blocked muscle bars.

## Starting token spec (extracted from `Daily Rep Blaze.dc.html`; the analysis workflow refines this)
Colors (most-frequent hexes in the Blaze export):
- **Backgrounds:** `#0B0D11` (app bg / deepest) · `#14171E` · `#1B1F28` (cards/surfaces) · `#1a1c20` (raised)
- **Blaze accent ramp:** `#FF4D2E` (hot) · `#FF5A2C` · `#FF6A3C` · **`#FF7A1E` (primary)** · `#FF8a4e` (soft) · `#FFB088` (tint) · burnt-dark `#1a0a04`/`#180a04` for accent-tinted fills
- **Text:** `#F4F6F8` (primary) · `#6b7178` (muted)
- **Secondary pops:** `#C6F24E`/`#C6FF8A` (lime), `#FACC4D`/`#fcd34d` (amber), `#7dd3fc` (sky), `#18A957`/`#0F7E40` (green — recovery)

Type:
- **Display / numerals:** `Archivo` (heavy / condensed-capable — drives the big headers + oversized numerals; see `blaze-archivo.png`)
- **Body / UI:** `Hanken Grotesk`
- (Spectral serif belongs to the Studio direction — NOT used for Charge.)

> NOTE: these are a starting point; the design-analysis workflow extracts the full, exact token set (incl. spacing scale, radii, shadows, motion, gradients) + the component styles. Verify against the actual `.dc.html` + screenshots before hard-coding.

## Phased implementation plan (one coherent, verified change per loop iteration)
**Phase 0 — Analysis (workflow):** extract the full token system + reusable-component inventory + per-screen mapping + current-codebase gap → write the detailed spec into this doc / `docs/redesign-blaze-plan.md`.

**Phase 1 — Token foundation ✅ DONE (2026-06-24):** design tokens defined as CSS variables in `globals.css` + wired into `tailwind.config` `theme.extend` (Blaze colors/fontFamily/fontSize scale/radii/shadow/gradients/spacing) + Archivo & Hanken Grotesk loaded via `next/font` on `<html>` (+`suppressHydrationWarning`). Alias strategy: legacy `--ink-*`/`--fg` values remapped to Blaze surfaces (so all 26 `*-lime`/`text-fg` files recolor for free); `--accent`/picker/`recovery.ts` left untouched so action+recovery stay lime until the primitives + iteration-6 picker decision flip action to orange. Verified: tsc + 336 tests + clean `next build` + live (bg #0B0D11, cards #14171E, text #F4F6F8, Hanken/Archivo, no flash, no console errors). See Shipped in loop-progress.md.

**Phase 2 — Reusable tokenized primitives:** build/upgrade the shared components every screen reuses — `Button`/CTA, `Card`/`StatTile`, `Chip`/`Pill`, section headers, the big-numeral stat, the color-blocked recovery bar — all consuming tokens (no hard-coded hex). Replace ad-hoc Tailwind classes with these primitives. +component/visual checks.

**Phase 3 — Screen-by-screen migration (IN PROGRESS — user directive 2026-06-24: "also implement the actual designs", i.e. build the real mockup layouts, not just token swaps):** Today (Home) hero + workout list → Progress (stat tiles, recovery, charts) → session logging → plans → library → settings → Auth. One screen per iteration, live-verified against the matching `blaze-*` screenshot, tests green. Reference: `Daily Rep Blaze.dc.html` (exact markup/tokens per screen) + `screenshots/blaze-full.png` (all 8 screens) + `today2.png`/`progress-row.png` etc.
- **it.1 ✅ Today hero → `BlazeHeroCard`** (orange "PUSH DAY" gradient hero: eyebrow+Switch, Archivo display title, dark-on-orange chips, nested dark numbered exercise list w/ focus lift, START CTA). All 3 states. Verified vs mockup.
- **it.2 ✅ Today recovery card → `RecoveryRing` + `RecoveryBar`** (conic donut 90% lime + 4 region bars Push/Pull/Legs/Core). Added `recoveryToken()` (kept `recoveryColor` ramp). **[corrected per user feedback]** the interactive BodyDiagram "muscle map" I'd kept below was REMOVED — the mockup's recovery card is only the ring + bars. Stick to the designs ([[stick-to-designs]]); BodyDiagram stays for Progress.
- **it.3 ✅ Today stat row → `ui/StatTile`** (3-up Last 7 days [orange]/Workouts/Week streak; replaced local StatCard). **Today now substantially Blaze** (hero+recovery+stats); minor Today bits left (Recommended Focus pills, Follow-a-Plan card) will update with Chip/Card.
- **it.4 ✅ Active Workout / session screen** (`session/page.tsx`: frosted sticky header, orange timer + `bg-gradient-blaze-h` progress, exercise cards done=recovery-fresh-check/numbered, superset bracket `border-blaze/30`, Add/Start/Finish via Button). Styling only. Verified planned+active states, mock restored.
- **it.5 ✅ Set Logging detail + shared `SetRow`** (frosted header, blaze coaching cue, raised steppers/done-circles, recovery-fresh done check + row highlight, blaze superset, Button quick-actions + NEXT). Styling only. Verified incl. done state; mock restored.
- **it.6 ✅ RestTimerBar (recovery-fresh accent) + Workout Summary** (orange check badge + shadow-icon, blaze PR banner, StatTile stats, blaze PR cards, orange DONE). Verified Summary live.
- **it.7 ✅ Progress screen** (PROGRESS title, 2×2 stats, orange volume bars, muscle balance Push-orange/Pull-sky/Legs-lime/Core, PRs). **User chose STRICT PARITY → stripped the recovery body-diagram + range selector + bodyweight chart + per-PR expand charts; DELETED dead `BodyDiagram` + its test (341 tests).** Verified.
- **it.8 ✅ Exercises/Library + `ui/Chip`** (Chip filter button [orange-active, a11y #14] + ChipTag; search aria-label #11; ExerciseCards w/ Compound-orange/equipment-neutral tags). Verified via DOM (screenshot tool times out on 49 cards). Kept the 2nd category-filter row (flagged).
- **it.9 ✅ History — list + Calendar** (uppercase cards, blaze 1RM, ChipTag muscles; Calendar = slim pace header + orange-trained month grid + legend, per the NEW mockup). **Design source updated → `Fitness app redesign styles (1)`** (adds Calendar/Plan-Builder/Legal). Verified.
- **it.10 ✅ Training Plans list** (display title, Chip filters, blaze active-banner, plan Cards w/ ChipTag tags + orange dots + Button CTAs, recommended/sheets). Verified.
- **it.11 ✅ Plan Builder** (settings card w/ Chip filters, day/lift editor w/ blaze superset badges + NumBox steppers, Button CTAs). Verified.
- **it.12 ✅ Settings** (`settings/page.tsx`) — restyled to the Blaze mockup (HTML 705-800): `font-display` uppercase "SETTINGS" title; Appearance card (Theme grid blaze-active `bg-blaze/[0.12] border-blaze/40 text-blaze-label`; **accent picker KEPT** — the mockup retains all 6 swatches, so the "accent-picker fate" open question is **resolved → keep**, no `theme.ts` change; restyled swatches to 34px + active double-ring `ring-fg ring-offset-bg` exactly per mockup); Profile/Gender + Goal/Experience `Choice` cards (blaze tint + gradient check badge); Units/Rest pills; Session-length numeral `text-blaze-warm` (#FF7A1E) + `accent-blaze` slider; Equipment/Focus `GridToggle` blaze; Account/Sample/Training-plan icon badges `bg-blaze/15 text-blaze-label`; Danger zone red→**rose** (`rose-400/40` border, `rose-400` icon, `rose-300` title) per mockup. Inactive surfaces follow the design's raised-in-card / card-on-page rule. **All handlers, a11y `role=radio`/`tabIndex`, decimal-input logic, reset/delete/sign-out/export flows preserved (visual-only).** ⚠️ **Concurrent billing work landed mid-iteration** (user added Stripe: `lib/billing.ts`, `store/useEntitlement.ts`, `MembershipSection` + checkout-return `useEffect` in Settings, `billing.test.ts`, edge fns) — merged cleanly with the restyle; I aligned the new Pro card's trophy badges lime→`bg-blaze/15 text-blaze-label`. Verified live (DOM + screenshot): SETTINGS title, Membership "Go Pro" gradient CTA, theme/accent/session/danger tokens correct. tsc clean, **353 tests** (9 new billing). Mock untouched (current=null, accent left as-is).
- **it.13 ✅ BottomNav polish** (`src/components/BottomNav.tsx`) — persistent chrome aligned to the mockup (HTML 791-796): container `bg-bg/[0.92]` + `backdrop-blur-md` (12px) + `border-t border-hairline/[0.08]`; **active tab decoupled from the accent picker** → fixed `text-blaze-warm` (#FF7A1E) font-bold stroke-1.9 (was `text-lime` = the saved picker accent, which rendered rose on the mock); inactive `text-fg/40` font-semibold stroke-1.6. Fast-Refresh applied cleanly (no restart). Verified live (DOM exact-match all 6 props + screenshot: "Today" tab orange). tsc clean, 353 tests. Mock untouched.
- **it.14 ✅ Legal (Terms + Privacy)** — restyled the shared `src/components/LegalPage.tsx` wrapper to the NEW Blaze reading-view mockup (HTML 1425-1456): chevron back-link "‹ Daily Rep" (`text-fg/50`); `font-display text-[26px] font-black uppercase` title; "Last updated" `text-fg/40`; prose `text-[14px] leading-[1.65] text-fg/70` with **Archivo** section headers (`[&_h2]:font-display [&_h2]:text-[15px] [&_h2]:font-extrabold`, non-uppercase), `[&_a]:text-blaze-label` links, disc bullets, `text-fg/90` strongs; footer email de-bracketed → `support@dailyrep.app` (blaze-label, per mockup). One wrapper edit restyles BOTH pages. **Legal COPY untouched** — existing counsel-`TODO` + `[Legal Entity]`/`[Jurisdiction]`/`[16]`/`[billing/refund]` placeholders preserved (still need a lawyer before charging, esp. the Subscriptions section now that Stripe billing is live). Verified live (DOM exact-match /terms + /privacy 9 sections + screenshot). tsc clean, 353 tests. Mock untouched (read-only nav, current=null).
- **it.15 ✅ ExercisePicker** (`src/components/ExercisePicker.tsx`, shared) — migrated the local muscle-filter `Chip` → the shared `ui/Chip` primitive (`selected` gradient fill + `text-on-accent` + a11y `aria-label="… filter, selected"`); cleaned legacy tokens: sticky header `bg-ink-800`→`bg-card`, search wrap `bg-ink-700 border-ink-600`→`bg-raised border-hairline/[0.08]` + `focus-within:border-blaze`, checkbox `accent-lime`→`accent-blaze`, add-badge `bg-lime/15 text-lime`→`bg-blaze/15 text-blaze-label`. Deleted the local `Chip` fn. Verified live in the plan builder (DOM exact-match: gradient selected chip, neutral raised unselected, orange focus-border search, blaze add-badge + screenshot). Fast-Refresh applied cleanly. tsc clean, 353 tests. Mock DB verified pristine afterward (current=null, 48 workouts, 0 custom plans — builder draft is local, never saved).
- **it.16 ✅ Sheet (shared modal chrome)** (`src/components/Sheet.tsx`) — bottom-sheet panel → `bg-card` + `border-t border-hairline/[0.08]`; drag handle `bg-ink-500`→`bg-fg/20`; close button `bg-ink-600/70 text-fg/80`→`bg-raised text-fg/70`; title `font-bold`→`font-extrabold`. Affects every sheet (ExercisePicker, plate-calc, plan sheets…). Verified live via ExercisePicker in the builder (DOM exact-match: panel #14171E, hairline top border, `fg/20` handle, raised close, 800 title). Fast-Refresh clean. tsc clean, 353 tests. Mock DB pristine (current=null, 48 workouts, 0 custom plans); exited builder via Plans→Discard.
- **it.17 ✅ Legacy-token sweep (5 files, parallel Workflow)** — final collision-safe sweep migrating remaining raw `ink`/`lime` tokens → Blaze tokens, one agent per file (ultracode multi-agent run, 5 agents / 45s): **`HomeSkeleton.tsx`** (19: `bg-ink-700/NN`→`bg-raised/NN`, `bg-ink-800/60`→`bg-card/60`, `border-ink-600/50`→`border-hairline/[0.08]`; layout confirmed already a faithful skeleton of the Blaze Today hero/recovery/3-stat, no rewrite), **`ToastHost.tsx`** (8: container `bg-card/95`+`border-hairline/[0.12]`, PR variant `bg-blaze/15`+`border-blaze/40`, icons + Undo → `text-blaze-label`/`bg-blaze/15`), **`SessionNote.tsx`** (3: textarea `bg-raised`/`border-hairline/[0.08]`/`focus:border-blaze`), **`session/[id]/page.tsx`** (2: bottom bar `bg-bg/90`+`border-hairline/[0.08]`), **`plans/page.tsx`** (1: orphaned-plan card `border-hairline/[0.12]`). `.pill` (structural alias) + `shrink-0` intentionally left. Verified centrally: tsc clean, 353 tests, zero residual real legacy tokens across all 5, code-reviewed the 2 meaty files (HomeSkeleton/ToastHost) + live /plans render check. Mock untouched.
- **it.18 ✅ Phase 4 brand — Charge Bolt** (resumed loop). Replaced the legacy lime-dumbbell app icon with the **Charge Bolt** mark (bolt path `M58 8 L28 54 H46 L42 92 L74 42 H54 Z`, orange gradient #FF4D2E→#FF7A1E on a #1A0A04 tile, per `Daily Rep Logo - Charge Bolt.dc.html`): rewrote `public/icon.svg` (vector favicon) + **regenerated all 4 PNG rasters via `qlmanage`** (the only rasterizer on the box; `rsvg/convert/sharp/inkscape` absent) — `icon-192.png`, `icon-512.png` (rounded full-tile), `icon-maskable-512.png` + `apple-touch-icon.png` (full-bleed, bolt padded into the maskable safe zone via a `-30 -30 160 160` viewBox); each visually verified (Read the rendered PNG) before copying into `public/`. Old PNGs backed up to `/tmp/cbicon/old/`. Added reusable **`src/components/BrandLogo.tsx`** (`<ChargeBolt>` bare mark + `<BrandLogo wordmark tagline>` lockup using `bg-on-accent` tile + `font-display` wordmark) for the upcoming Auth/Onboarding hero, with `CHARGE_BOLT_PATH` exported + unit-tested (`brandLogo.test.ts`, **354 tests**). Manifest + `layout.tsx` icon wiring unchanged (no regression). Verified: tsc clean, served `/icon.svg` + all 4 PNGs + manifest over HTTP. NOTE: vitest has NO `@/` alias — component tests must use relative imports (`../components/...`), like `button`/`chip` tests.
- **it.19 ✅ Auth** (`src/screens/Auth.tsx`) — restyled to the SIGN UP/SIGN IN mockup (HTML 913/952): full-width **`bg-gradient-blaze` hero** with the `<BrandLogo size={74}>` Charge Bolt tile + a mode-dependent `font-display text-[34px]` title (`Daily Rep` / `Welcome back`, `text-on-accent`) + uppercase tagline (`text-on-accent/70` — `/60` would be ~4.0:1 on the orange, bumped for AA); an overlapping `bg-bg rounded-t-[28px] -mt-[22px]` form sheet with a segmented Create-account/Sign-in **tab switcher** (active = `bg-gradient-blaze text-on-accent`), token `bg-card border-hairline/10 focus:border-blaze` email/password inputs **+ a new show/hide password eye-toggle**, gradient `<Button size="lg">` CTA, `text-blaze-label` mode-switch link, Terms/Privacy + sync notes. **Social (Apple/Google) buttons OMITTED** — read `Auth.tsx` first and confirmed the app is **email/password only** (Supabase + Turnstile); the mockup's social buttons aren't wired, so shipping them would be dead UI (a feature, not a restyle). **ALL auth logic preserved verbatim** (signUp/signIn, mode toggle, Turnstile `captchaNonce` remount, error/pending, telemetry). Verified: tsc clean, 354 tests, zero residual legacy tokens. ⚠️ **Could NOT live-preview:** Auth only renders signed-out, and BOTH safe paths are blocked — hiding the Supabase session (`daily-rep-sb-auth`) was **denied by the safety classifier** (mock-protection boundary), and a temp public-preview route would require editing `AppShell.tsx` which is **billing-adjacent** (imports `useEntitlement`/`Paywall`, holds the entitlement gate). So verified via tsc + tests + code review + the fact it's composed entirely of already-shipped-verified primitives (BrandLogo≡favicon render, Button/gradient/card-inputs/blaze-label). **User should do a quick visual pass on the signed-out Auth screen.** Mock session confirmed untouched (token intact).
- **it.20 ✅ Onboarding + OG image** — final redesign iteration.
  - **Onboarding** (`src/screens/Onboarding.tsx`, 7-step wizard) restyled to Blaze: progress dots filled `bg-gradient-blaze` / empty `bg-raised`; `font-display` uppercase step titles (`Section` + Welcome + Review); Welcome lockup `<BrandLogo tile="gradient" size={60}>` (added a **`tile="gradient"`** variant to `BrandLogo` — orange tile + dark bolt, the brand "Primary" lockup — so it reads on the dark bg; default `tile="dark"` keeps Auth unchanged); all `Choice`/option/gender/units/days/equipment/preset cards → blaze-active (`border-blaze/40 bg-blaze/10 text-blaze-label` + gradient check), muscle tri-state emphasize→blaze / avoid→**rose-400**, session numeral `text-blaze-warm` + `accent-blaze` slider, inputs `bg-card border-hairline/10 focus:border-blaze`, Review summary `divide-hairline/[0.08]` + `ChipTag` plan tags + `<Button>` CTAs, sticky nav `bg-bg/[0.92] backdrop-blur-md` with `<Button>` back/Continue. **ALL onboarding logic preserved verbatim** (step state, Skip, editable review jump-back, tri-state `cycleMuscle`→avoidMuscles, daysPerWeek, `recommendPlans`/Start-plan, `nameFromEmail`, commit's `focusMuscles: focus.filter(!avoid)`). Gated screen → code-verified (tsc + 354 tests + verified-primitive composition); **user should visually review** (can't preview without flipping the mock's synced `onboarded`).
  - **OG image** (`public/og.png`, 1200×630): replaced the old lime-dumbbell with a **Charge Bolt** banner (gradient tile + dark bolt + "DAILY REP" + "Training that adapts to your recovery.") — authored an OG SVG, rasterized via `qlmanage` (renders SVG text fine), then `sips -c 630 1200` to crop the square render to 1200×630; visually verified (Read the PNG). Old backed up to `/tmp/cbicon/old/og.png`. Metadata wiring (`layout.tsx` openGraph/twitter → `/og.png`) unchanged.
- **🎉 REDESIGN COMPLETE.** Charge/Blaze redesign DONE across **every** screen + shared chrome + brand (icons/favicon/OG) + Auth + Onboarding. Verified each iteration: tsc(src) clean, **354 vitest tests**, live preview where possible. **Follow-ups for the user / [HUMAN]:** (a) **visual pass on the two gated screens** (Auth, Onboarding) — couldn't be previewed against the protected signed-in mock. (b) **App-icon treatment choice:** the favicon/PNG icons use the **"Knockout"** variant (dark tile + gradient bolt); the brand sheet labels **gradient-tile + dark-bolt as "Primary"** (now available via `<BrandLogo tile="gradient">`) — decide which the home-screen icon should be (regen rasters via `qlmanage` if switching). (c) billing UI (`Paywall`/`TrialBanner`/`MembershipSection`) is the **user's** code — restyle to Blaze when ready (not touched). (Loop stopped at it.17 per user request, resumed, now complete through it.20 → STOPPING.)

**Phase 4 — Brand:** swap in the "Charge Bolt" logo/wordmark (favicon, PWA icons, OG, Auth header, manifest) once a designer-exported asset is available (`[HUMAN]` for final raster art; scaffold from the `.dc.html` SVG meanwhile).

## Guardrails
- Token-driven: NO new hard-coded hex/spacing in components — everything references a token.
- Reusable: extract a primitive the moment a pattern repeats; screens compose primitives.
- Never regress: keep `tsc` + `vitest` green every iteration; preserve the progression engine, analytics projection, sync (monotonic clock + retry), auth gate, PWA, charts. AA contrast must hold (the blaze orange on dark + muted-text tiers need a contrast check).
- Verify each screen live against its screenshot on the 48-session seeded mock account.

---

# Detailed implementation plan (analysis workflow `wjwkh1463`, 2026-06-24)

> **Decision:** Replace the lime-on-ink look with **Charge/Blaze** (hot-orange action accent, **lime reserved for recovery**, Archivo + Hanken Grotesk type), token-driven: a CSS-variable layer feeds Tailwind utilities + a small set of reusable primitives. `.dark`/`.light` mechanism preserved.
>
> **Alias strategy (the key to not breaking 26 files):** keep the existing `--ink-*`/`--fg`/`--accent` var NAMES — 26 files use `text-lime`/`bg-lime`/`text-fg/*`. Remap the *values* of `--ink-*`/`--fg` to Blaze surfaces. Add a parallel **Blaze namespace** (`blaze`, `recovery`, `split`, `bg/card/raised/hairline`, `on-accent`) that new primitives read from. Action surfaces flip to orange progressively as primitives (using `blaze` tokens) replace `*-lime` call sites, screen by screen.

### Grounding (verified in-repo)
- Token layer: `globals.css:5-28` — RGB triples `--ink-900…-400`, `--fg`, `--accent` (`.dark` + `.light`); **`--accent` is overwritten inline by the accent picker** (`theme.ts:37-45`) + the no-flash script (`layout.tsx:50-55`) → flipping base `--accent` in CSS has no visible effect until the picker is addressed (iteration 6).
- Tailwind: `tailwind.config.ts:7-37` — `ink.*`/`fg`/`lime`/`accent` → all point at the vars; `fontFamily.sans` = system stack.
- Component classes: `globals.css:73-101` — `.card`, `.btn*`, `.pill`. No React primitives yet.
- No `next/font`. Recovery color = runtime HSL ramp (`recovery.ts:109-116`). AA fixups `globals.css:141-145` (`.light .text-fg/*`).
- 22 vitest suites are logic/a11y only — **none assert styling**, so token work won't break them; keep it that way.

### 1. Token spec
**Surfaces/text (RGB triples, `R G B`):** `--color-bg` 11 13 17 (#0B0D11, →`--ink-900`) · `--color-card` 20 23 30 (#14171E, →`--ink-800`) · `--color-raised` 27 31 40 (#1B1F28, →`--ink-700`) · `--color-border` 255 255 255 (use /0.08) · `--fg` 244 246 248 (#F4F6F8, softened from pure white). Light overrides: bg 247 248 250, card 255 255 255, raised 240 242 245 (**already the current light `--ink` values** — light surfaces need no change). Text tiers stay alpha-on-`--fg` so the `.light .text-fg/*` AA maps keep working.
**Accent/action:** `--color-accent` 255 90 44 (#FF5A2C; light 194 65 12 #C2410C) · `--color-accent-hot` 255 77 46 · `--color-accent-warm` 255 122 30 · `--color-accent-label` 255 138 78 (#FF8A4E — use for accent *text/eyebrows* on dark, ≈6.8:1) · `--color-on-accent` 26 10 4 (#1A0A04 text on orange fills) · `--gradient-blaze` linear-gradient(135deg, #FF4D2E, #FF7A1E) · `--gradient-blaze-h` 90deg same.
**Recovery/semantic (mode-invariant):** `--color-recovery-fresh` 198 242 78 (#C6F24E — **recovery ONLY, never action**) · `--color-recovery-moderate` 250 204 77 · `--color-recovery-rest` 251 113 133 · `--color-pull` 125 211 252 · `--color-core` 252 211 77 · `--color-legs` 244 114 182.
**Type:** `--font-display` = Archivo (oversized numerals, display, CTA) · `--font-body` = Hanken Grotesk (body/UI, becomes `font-sans`). Named scale (size/weight/tracking/lh) wired as Tailwind `fontSize`: display-lg 40/900/-0.02em/0.9·uppercase, display-md 30/900/-0.02em/0.95, display-sm 26/900, num-lg 40/900/1, num-md 26/900/1, num-sm 24/900, num-xs 21/900, heading-lg 26/800, heading-md 23/800/-0.02em, heading-sm 19/900·uppercase, body-lg 15/600, body-md 14/600, body-sm 13/600, label-md 12/800/0.1em·uppercase, label-sm 11/800/0.12em·uppercase, chip 12/700/0.03em·uppercase.
**Radii:** sm 8 · md 13 (button) · card 16 · lg 18 · hero 22 · full 999. **Shadows:** button `0 8px 20px rgb(255 90 44/.28)` · button-lg `0 10px 26px /.30` · hero `0 14px 34px rgb(255 77 46/.26)` · icon `0 12px 30px rgb(255 90 44/.35)`. **Spacing tokens:** hero 18 · gutter 20 · group 26. **Motion:** `--blur-overlay` blur(12px) · `--ease-standard` cubic-bezier(.2,0,0,1) · dur-fast 150ms · dur-base 250ms (existing reduced-motion block left intact).
**AA call-outs (verify, don't assume):** orange fill #FF5A2C as *text* on #0B0D11 ≈4:1 (borderline) → use `--color-accent-label` #FF8A4E for accent text, reserve #FF5A2C for *fills* with #1A0A04 text (≈8:1). Re-verify `/35` muted text now that `--fg` is #F4F6F8. Recovery tints as text need ≥4.5:1.

### 2. Tailwind wiring
- **`layout.tsx`:** `next/font/google` Archivo (weights 400–900, var `--font-archivo`) + Hanken_Grotesk (400–800, var `--font-hanken`), `display:'swap'`; put `${archivo.variable} ${hanken.variable}` on `<html>`; keep `font-sans` on `<body>`. Self-hosted → offline/PWA + no-flash script unaffected.
- **`tailwind.config.ts` `theme.extend`:** KEEP `ink`/`fg`/`lime`/`accent`. ADD colors `bg/card/raised/hairline`, `blaze.{DEFAULT,hot,warm,label}`, `on-accent`, `recovery.{fresh,moderate,rest}`, `split.{pull,core,legs}`; `fontFamily.{sans→Hanken, display→Archivo}`; the named `fontSize` scale; `borderRadius.{card,hero,blaze}`; `boxShadow.{button,button-lg,hero,icon,1}`; `backgroundImage.{blaze,blaze-h}`; `spacing.{hero,gutter,group}`. Net: no utility changes meaning except color → builds stay green.

### 3. Reusable primitives (build in `src/components/ui/`, tokens only)
Priority: 1 `Button` (primary=`bg-gradient-blaze`+`text-on-accent`+`shadow-button`+`rounded-blaze` uppercase 900, secondary=`bg-raised`+`border-hairline/10`, ghost) → 2 `Card` → 3 `Chip`/`Pill` (active=blaze tint, recovery=lime tint) → 4 `SectionHeader` (eyebrow `text-label-sm`) → 5 `StatTile` (big `font-display` numeral) → 6 `RecoveryBar` (refactor `recovery.ts:recoveryColor()` to return a **token name**, color in one place) → 7 `RecoveryRing` (conic donut) → 8 `ExerciseRow` → 9 `BlazeHeroCard` (flagship gradient hero) → 10 `BottomNav` (active `text-blaze`, overlay `bg-bg/92` + `--blur-overlay`) → 11 `Sheet`/sticky chrome (frosted) → 12 `RestTimerBar` → 13 set-logging table states. Legacy `.btn*`/`.card`/`.pill` stay as thin aliases until call sites migrate.

### 4. Screen migration order (one screen / iteration, live-verified vs `blaze-*.png`)
1 **Today** `app/page.tsx` (+`HomeSkeleton`) — seeds every primitive. 2 **Progress** `app/progress/page.tsx` — StatTile 2×2, volume bars (`bg-blaze-h`), muscle-balance RecoveryBar. 3 **Session** `session/{page,[id],summary}`, `SetRow`, `RestTimerBar`, `SessionNote` — highest functional risk, **styling-only, keep logging/progression + tests**. 4 **Plans** `plans/{page,builder}`. 5 **Library** `library/page.tsx`, `ExercisePicker`. 6 **Settings** `settings/page.tsx` — **decide accent-picker fate** (recommend: lock action to Blaze, repurpose/remove the 6-accent picker; edits `theme.ts` `ACCENTS`/`applyTheme` + the no-flash `A` map). 7 **Auth/Onboarding** `screens/{Auth,Onboarding}.tsx` — brand lockup. History/legal fold in opportunistically.

### 5. Phase 1 — token foundation (this iteration)
**Files:** (1) `globals.css` — remap dark `--ink-*`+`--fg` to Blaze surfaces; add the full additive Blaze token set; add `--font-display`/`--font-body`; `body` font-family `var(--font-body)` fallback. Light surfaces already match Blaze light values → unchanged; `.light .text-fg/*` AA maps untouched. (2) `tailwind.config.ts` — §2 extend (keep ink/fg/lime/accent). (3) `layout.tsx` — `next/font` Archivo+Hanken on `<html>`.
**`--accent`/picker/`recovery.ts` UNTOUCHED this iteration** — accent stays user-controlled (lime default), so recovery `*-lime` usages STAY lime (correct) and the picker keeps working; the orange action identity lands progressively via the `blaze`-token primitives (§3) + the iteration-6 picker decision. This keeps Phase 1 a pure, non-regressing substrate: Blaze dark surfaces + Blaze typography land, nothing breaks.
**Verify gate:** `tsc --noEmit` + `vitest run` (22 suites, must stay green) + real `next build` (font/config) + live: Archivo on numerals/headers, Hanken on body, bg #0B0D11 / cards #14171E / text #F4F6F8, no theme flash, picker + recovery-lime intact, reduced-motion still neutralized.

### Open questions (resolve at the relevant iteration)
- **Accent-picker fate** (Settings, it.6): keep 6-accent (dilutes brand) vs lock to Blaze (recommended). Defer.
- **Recovery color tokenization:** fold `recovery.ts:recoveryColor()` → token thresholds when building `RecoveryBar` (it.6); adjust any color assertion in `recovery.test.ts` to the token string.
