# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*

# next.js / build output
/.next/
/out/
/build
*.tsbuildinfo
next-env.d.ts

# testing / coverage
/coverage

# /ship pipeline — per-run handoff scratch (keep the README, ignore the artifacts)
/.pipeline/*
!/.pipeline/README.md

# env files — NEVER commit real secrets (keep .env.example tracked)
.env
.env.*
!.env.example

# misc / os
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# editor
.vscode/*
!.vscode/extensions.json
.idea
